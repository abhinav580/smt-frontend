# Save exact versions of installed packages
save-exact=true

# Use a specific registry (optional, defaults to npmjs.org)
registry=https://registry.npmjs.org/

# Set the prefix for global installs (optional)
# prefix=${HOME}/.npm-global

# Strict SSL for secure downloads
strict-ssl=true

# Always show progress during install
progress=true

# Cache folder location (optional)
# cache=${HOME}/.npm

# Optional: Increase fetch-retry attempts
fetch-retries=3
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000